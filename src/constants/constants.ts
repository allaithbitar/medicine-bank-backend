export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_PAGE_NUMBER = 0;

export const ACTIONER_WITH = {
  columns: {
    id: true,
    name: true,
  },
};

export const PAYMENTS_START_DATE = "2026-02-12";
